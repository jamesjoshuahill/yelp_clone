%h1
  Edit
  = @restaurant.name
= form_for @restaurant do |f|
  = f.label :name
  = f.text_field :name, placeholder: "Name of the restaurant"
  = f.label :description
  = f.text_field :description, placeholder: "Describe the restaurant"
  = f.submit 'Edit restaurant'